name: Yosys

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Install dependencies
      run: |
        sudo apt install yosys
    - name: Running TinyFPGABX Synth
      run: |
        make synth BOARD=tinyfpgabx
